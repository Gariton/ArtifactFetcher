# ArtifactFetcher — デザイン刷新プロンプト（Claude Design 用）

> このファイルの本文をそのまま Claude Design に貼り付けてください。

---

あなたはプロダクトデザイナーです。既存の社内向けWebツール **ArtifactFetcher** のUIを刷新したい。現状は UIライブラリ（Mantine）のデフォルトテーマのままで、装飾・ブランド・情報設計が弱い。機能はそのままに、デザインシステムと主要画面のビジュアルを一新してほしい。

## プロダクト概要

ArtifactFetcher は、**オフライン/閉域（エアギャップ）環境やプロキシ制限のある社内ネットワーク**のために、外部レジストリから成果物を「依存関係ごと」サーバー側で取得し、進捗をリアルタイム表示しながら、(1) 1つのアーカイブとしてダウンロード、または (2) 社内のプライベートレジストリへアップロード/push できるツールです。

対応する成果物（5種類）:
- **Docker イメージ** — 任意レジストリから pull → load 可能な tar を生成 / tar から任意レジストリへ push
- **npm パッケージ** — lockfile もしくは `name@semver` から依存を全解決し全 tarball を取得 / 任意レジストリへ publish
- **pip パッケージ** — PyPI や社内インデックスからまとめて取得 / アップロード
- **rpm パッケージ** — 公式リポジトリ/EPEL から依存込みで収集 / 任意 RPM リポジトリへアップロード
- **Hugging Face モデル** — GGUF など必要ファイルだけ選択取得し Ollama 等で使えるアーカイブ化

## ターゲットユーザー / トーン

- 開発者・SRE・インフラ/プラットフォーム担当。閉域環境で日々の構築・移送作業に使う。
- 「速くて信頼できる実務ツール」。派手さより**情報の見やすさ・操作の確実性・進捗の透明性**。
- UI言語は日本語主体（英語の固有名詞・技術用語は英語のまま）。
- ライト/ダーク両対応必須。ダークがデフォルト（auto）。

## 技術制約（デザインはこの枠内で実装される前提）

- Next.js (App Router) + **Mantine v7/8** + **@tabler/icons-react**。
- 提案は Mantine のテーマトークン（色・radius・spacing・fontFamily・components default props）で表現できる範囲を基本とし、必要なら CSS Modules での装飾も可。
- レスポンシブ必須。デスクトップはヘッダー内ナビ、モバイルはバーガー→ドロワーナビ。

## 情報設計（既存。維持してほしい）

1. **ホーム `/`** — アプリアイコン + タイトル「Artifact Fetcher」 + 5つの成果物マネージャのカタログ。現状は円形アイコンボタン→モーダルで説明＋遷移。ここを刷新の目玉にしたい。
2. **各マネージャ画面** `/docker` `/npm` `/pip` `/rpm` `/hf` — 共通レイアウト:
   - 見出し（タイトル＋大きめアイコン）＋ 1行の説明
   - **タブ:「ダウンロード」/「アップロード」**（pill 形式）
   - ダウンロード面: 折りたたみの「接続/認証設定」（レジストリURL・ユーザー名・パスワード/トークン・TLS検証オフ等）＋ 入力フォーム（Dockerなら repo/tag/platform、npm/pip/rpm なら パッケージ指定 or lockfile）＋ 実行ボタン → 進捗モーダル
   - アップロード面: `.tar`/`.tgz` のドロップゾーン ＋ ファイル一覧（ファイルごとに進捗バー・状態）＋ アップロード先設定 ＋ 実行
3. **進捗モーダル（最重要 UX）** — SSE でリアルタイム更新。アイテム/レイヤごとのカード: 進捗バー、サイズ、状態（処理中=スピナー / 完了=チェック / スキップ済=バッジ / エラー）。複数同時進行・大量項目（数十〜数百）でも破綻しない一覧表示。
4. **管理 `/admin`** — リクエストログのテーブル（任意で表示）。
5. **共通ヘッダー** — アプリタイトル、主要ナビ（docker/npm/pip…）、GitHubリンク、ライト/ダーク切替。
6. **フッター** — `© 2025 Gariton_`。

## デザインで解決したい課題

- 現状トップの「円形アイコン＋ツールチップ＋モーダル」は各ツールの違いや用途が一目で伝わらない。**5マネージャを役割・色・説明込みで一覧できるカード型カタログ**へ。
- ダウンロード/アップロードのフォームが素朴。**入力の主従（必須 vs 詳細設定）が分かる階層**と、認証情報の安全な見せ方。
- 進捗表示が単調。**長時間処理の安心感**（全体進捗、残り/完了数、スループット、失敗時のリトライ導線）を与えるダッシュボード的な進捗体験。
- 5種の成果物に**一貫したアクセントカラー体系**を与える（例: docker=blue, npm=red, pip=violet, hf=teal, rpm=yellow を活かしつつ調和させる）。

## 望むビジュアル方向（たたき台。提案で上書き可）

- モダンで端正な「デベロッパーツール」感。余白広め、角丸は中〜大、カード主体、微細な境界線と控えめな影。
- ニュートラルなグレースケール基調＋成果物ごとのアクセント1色。等幅フォントを SHA/バージョン/コマンド等のメタ情報に効かせる。
- ダーク基調で映える配色。状態色（成功/警告/エラー/スキップ/進行中）を明確に。
- アイコンは Tabler 系の線アイコンで統一（stroke 細め）。

## 成果物として出してほしいもの

1. **デザインシステムの提案**: カラートークン（ライト/ダーク両方、成果物アクセント含む）、タイポグラフィスケール、spacing/radius、状態色、Mantine テーマ設定への落とし込み方針。
2. **主要画面のハイフィデリティ・モックアップ**（最低限）:
   - ホーム（マネージャカタログ）
   - マネージャ画面のダウンロードタブ（フォーム＋詳細設定折りたたみ）
   - 進捗モーダル（複数項目が走っている状態 / 完了 / 一部失敗の3状態）
   - アップロードタブ（ドロップゾーン＋ファイル一覧進捗）
3. 主要コンポーネントの状態バリエーション（ボタン/入力/カード/タブ/進捗カードの hover・focus・disabled・error）。
4. レスポンシブ指針（デスクトップ/モバイルでのナビとレイアウトの違い）。

まずデザインの方向性を2〜3案のコンセプト（ムード/配色のサムネイル）で提示し、私が1案選んだら主要画面へ展開してください。
